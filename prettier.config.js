const config = {
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'none',
  semi: false,
  singleQuote: true,
  printWidth: 80,
  overrides: [{ files: ['*.json', '*.js'], options: { tabWidth: 2 } }],
  arrowParens: 'avoid'
}

module.exports = config
